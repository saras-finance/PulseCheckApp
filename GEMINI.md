# PulseCheck Mandates

## Python Environment
- **Virtual Environments Only:** Always use a Python virtual environment (`.venv`) for any script execution. Never install or use system-level Python packages.
- **Dependency Management:** Maintain dependencies within the virtual environment and ensure it is created in the project root if it doesn't exist.
